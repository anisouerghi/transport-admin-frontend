import { NgModule } from '@angular/core';
import { UserRoutingModule } from './user-routing.module';

/** Module feature Administration Users (CRUD). */
@NgModule({
  imports: [UserRoutingModule],
})
export class UsersModule {}
