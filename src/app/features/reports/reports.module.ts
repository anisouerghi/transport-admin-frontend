import { NgModule } from '@angular/core';
import { ReportRoutingModule } from './report-routing.module';

/** Module feature Reports (Signalements). */
@NgModule({
  imports: [ReportRoutingModule],
})
export class ReportsModule {}
