/** Vue d'ensemble statistiques (squelette extensible). */
export interface StatisticsOverview {
  totalReports: number;
  totalSupports: number;
  totalUsers: number;
  totalPassengers: number;
  activePassengers: number;
  inactivePassengers: number;
}
